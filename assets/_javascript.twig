
<script type="text/javascript">
    var asyncpath = "{{ basepath }}";
    var tooltipvalues = "{{ tooltips }}";
    var lock_after_vote = "{{ config.lock_after_vote }}";
    $('.rateit').bind('over', function(e, value) {
        $(this).attr('title', tooltipvalues[value - 1]);
    });
</script>
